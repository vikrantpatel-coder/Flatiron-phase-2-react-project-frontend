
import styled from 'styled-components';

const StyledImage = styled.img`
  width: auto; 
  height: auto; 
  filter: ; grayscale(0.05) brightness(1.2) contrast(1.2);
  border-radius: 30px;

  `

export default StyledImage;